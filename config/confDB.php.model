<?php
namespace config;
// Class used for database connection configuration
class confDB {
	const host = "127.0.0.1";
	// const user = ""; // comment it if no user set
	// const password = ""; // comment it if no password set
	const keyspace = "cloud";
}
